version: "privacy-example"
defaults:
  strategy: priority
  fallbackAllowed: false
routes:
  - id: restricted
    when: {}
    require:
      tags: [private]
    select:
      strategy: priority
      candidates: [demo-private]
