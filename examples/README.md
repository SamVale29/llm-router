# Examples

The examples are decision-only unless they explicitly construct an adapter. The model catalog is fictional and illustrative for demonstrating policy behavior.

Run the root checks first, then use tsx against the TypeScript examples or the CLI against the YAML policies.
