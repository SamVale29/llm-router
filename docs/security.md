# Security

Use HTTPS endpoints and endpoint allowlists. Keep the proxy on localhost unless an internal token, CORS allowlist, payload limit and rate limit are configured. Do not put a provider key in the hosted playground.

The repository runs dependency review, CodeQL, secret scanning and lockfile checks where supported. Report vulnerabilities privately through the GitHub security flow.
